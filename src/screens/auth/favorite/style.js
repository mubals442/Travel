import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  Favorite: {
    SafeAreaView: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      backgroundColor: 'red',
    },
    Text: {
      color: '#ffffff',
    },
  },
});
